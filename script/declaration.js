    // 체크인
    let checkInValue;
    let receiptCheckIn = document.querySelector('div#receipt-checkIn');
    // 체크아웃
    let checkOutValue;
    let receiptCheckOut= document.querySelector('span#receipt-checkOut');
    // 객실
    let roomSizeValue;
    let receiptRoom = document.querySelector('div#receipt-room');
    // 요금
    let roomFeeValue;
    // 인원
    // 성인
    let adultValue;
    let receiptAdult = document.querySelector('span#receipt-adult');
    // 아동
    let childValue;
    let receiptChild = document.querySelector('span#receipt-child');